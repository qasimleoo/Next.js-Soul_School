const SlugLayout = ({ children }) => {
    return (
        <div>
            <p className="text-red-400">Server Layout goes here</p>
            {children}
        </div>
    );
};

export default SlugLayout;
